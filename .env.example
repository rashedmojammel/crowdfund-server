# Same MongoDB as the client repo — BetterAuth writes users there, server reads them
MONGODB_URI=

# Must match the client repo exactly — client signs JWTs, server verifies
BETTER_AUTH_SECRET=

# Stripe
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Comma-separated origins allowed by CORS middleware
ALLOWED_ORIGINS=http://localhost:3000
